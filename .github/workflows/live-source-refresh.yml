name: Live Source Refresh

on:
  schedule:
    - cron: "3,18,33,48 * * * *"
  workflow_dispatch:

permissions: {}

jobs:
  production:
    if: vars.HI_PRODUCTION_AUTOMATION_ENABLED == 'true' && github.repository == 'Wesleyk31/Hire-Intelligence' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      id-token: write
    concurrency:
      group: hirer-production-automation
      cancel-in-progress: false
      queue: max
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "22"
      - name: Run bounded production live-refresh
        run: node scripts/automation/runner.mjs live-refresh
