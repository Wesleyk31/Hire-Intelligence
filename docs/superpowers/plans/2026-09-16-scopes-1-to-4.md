# Hire Intelligence scopes 1–4 implementation

User authorization: “so fix everythign then continue scope 1 to 4”, following docs/audit/NEXT_SCOPE.md.
Continue in the existing Hire Intelligence project and branch. Preserve the original handoff and all evidence. Implement independent areas in parallel; root integrates and runs the complete suite.

## Scope 1 — release blockers
- [x] Recover intact homepage artwork from this app's version history. If unavailable, restore rendering with new clearly documented industry artwork; do not call replacement artwork the recovered approved original.
- [x] Confirm current AppDeploy SDK constraints, auth and storage boundaries, cron status and deploy contract.
- [ ] Prepare current production source and platform workflow tests. Keep synthetic SDK/test aliases out of deployment. Deploy an isolated staging app with cron disabled, then exercise anonymous denial, real auth and storage when account access permits.
- [ ] Record rollback version, QA results and release decision. Do not claim provider login/two-account tests passed from mocks. Missing business/account inputs remain explicit.

## Scope 2 — historical coverage and data quality
- [x] Add bounded continuation pagination over current rows and archive page/event offsets, with strict token validation and cycle detection. Return original provenance and source dates, quality findings and next cursor.
- [x] Add an authenticated evidence browser with Previous/Next, origin filter, row details and download of the reviewed page. Rankings remain explicitly scoped; do not claim a global complete index from a partial window.
- [x] Add dry-run inventory tooling for malformed AEMO rows, missing/positional identities, duplicate source IDs and invalid/stale dates. Preserve original records and produce reviewable findings before quarantine.
- [x] Harden current-feed persistence acknowledgements and write byte limits; pin bounded per-source indexes and disclose/stop oversized indexes instead of losing mappings.

## Scope 3 — feed recovery and pilots
- [x] Revalidate corrected existing collectors with read-only bounded probes; update exact WFS layers and recorded health. Provider emptiness/access failures stay degraded.
- [x] Implement Logan DA and QLD coordinated-project contracts, stable IDs, quality quarantine, context-only semantics, attribution and tests.
- [x] Integrate read-only pilot previews and observable ingestion acceptance; do not promote unresolved flags or manufacture current-demand dates.
- [x] Keep unresolved licences/access clearly gated and record the exact next dependency.

## Scope 4 — operations, performance and acceptance
- [x] Expose duration, duplicates, date completeness and persistence failures in source-health results. Check quota propagation and bounded cron work.
- [x] Split operational app, map and PDF loading; verify loading/error states and measure output chunks.
- [ ] Exercise representative data volume, accessibility/keyboard, mobile layouts, generated PDFs and user-owned account persistence.
- [ ] Complete truthful Privacy/Terms and retention/access wording using supplied business details; request missing legal entity/admin/retention facts without blocking independent coding.

## Tests and preflight
Add behavioral tests before fixes: failed state/index acknowledgements, SDK quota propagation, pagination reaching records beyond the existing window, malformed/invalid tokens, partial archive pages, duplicate/malformed inventory, source pilot schema/quality cases and deferred UI loading.
Use actual handlers/normalizers with only network/database boundaries replaced. Run all unit tests, frontend/backend types, full Chrome suite, PDF bounds, static hardening checks, original SHA256 hashes and verification build. Browse current official source docs before new provider integrations.
Before any AppDeploy call: latest SDK docs read, changed files only, tests/tests.txt reconciled for updates or 3–5 complete tests/tests.json workflows for new staging, one sanity workflow, every changed API covered or explicitly internal, no secret values, no verification runtime, no unsupported SDK operations, all local preflight checks passing. Deployment must be followed to terminal status and QA errors investigated.

## Delivery
Keep each scope status honest: implemented locally, validated on staging, provider-blocked or business-input-blocked. Save measured evidence and a local commit; do not mark all four accepted until their actual external acceptance checks pass.

## Execution result

Local implementation and checks are delivered; unchecked items retain real-runtime/business acceptance work. Staging request was blocked by automatic approval review (200,000-byte limit). See docs/audit/SCOPES_1_TO_4_PROGRESS.md for exact implemented/blocked boundaries. Live mutation, real account persistence, distributed writer guarantees and approved legal details are not claimed.
