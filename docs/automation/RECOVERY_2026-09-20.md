# Economy-mode recovery — 20 September 2026

The user selected fewer scheduled data pulls after the hosting-allowance incident. Public API availability and the real authenticated production API/browser checks were verified in [run 35480805868](https://github.com/Wesleyk31/Hire-Intelligence/actions/runs/35480805868). Release checks below are distinct from that earlier live run.

## Implementation and release checklist

- Keep the existing AppDeploy frontend/backend application, managed database, source identities, evidence versions and checkpoints.
- Repair false repeated-page failures in Melbourne building permits and AEMO KCI. Validate their publisher-declared schemas before clearing pagination holds; genuine access restrictions and schema changes stay held.
- Prevent duplicate pending sign-in/sign-out operations and permit retry after failure. Cover these transitions in browser acceptance and reconcile the existing native TXT test suite.
- Read one backfill snapshot per health response and retain the real metric-basis disclosure. Add bounded, redacted source/job failure diagnostics.
- Align GitHub schedules, backend policies and watchdog thresholds with economy mode: 24 scheduled runs/day instead of 269, excluding manual/event-triggered runs.
- Release changed files only, run controlled production jobs, and record their actual conclusions before enabling schedules.

## Deployment preflight

- Existing app `hirer-intelligence-mfj58p`, name Hire Intelligence, type `frontend+backend`; React/Vite hash routing is retained.
- Deployment and auth/API/database SDK contracts reviewed. Existing supported SDK methods are retained; no new credentials, resources, SDK casts or dependency changes.
- Incremental diffs for six changed files are checked against the applied snapshot `1789694012430`; each anchor is unique and reapplying the diffs reproduces the local files. The payload is approximately 20 KB.
- Native test 1 now covers authentication transitions and duplicate pending actions. Internal automation operations are covered by executable backend/runner tests rather than public UI writes.
- No database reset, destructive migration, source admission bypass or native cron restart. GitHub production writers stay paused during deployment.
- Before deployment, confirm unit tests, frontend/backend types, verification build, workflow/schema/format checks, browser acceptance and independent review. After deployment, inspect native status/errors and actual GitHub production results.

## Source observations

Release verification: 443 unit tests and 117 local browser checks passed, both TypeScript projects checked successfully, and the verification build completed. Workflow safety, static schema/migration checks and automation formatting passed. Independent review found no actionable findings. Local browser tests no longer include the live-only production smoke test.

Both repaired probes retrieved two full 100-row pages from the official publisher on 20 September: schema, parser and pagination passed. Freshness remains `NOT_VERIFIED`. The [read-only audit](../audit/SOURCE_PROBE_RECOVERY_2026-09-20.json) includes timestamps, schema hashes and evidence of legitimate repeated IDs.

The production health snapshot contains two Queensland sources held for provider challenges and 13 configured sources without stored observations. They remain outside the 55 successful eligible-source checks. Subsequent live observations and final workflow enablement are recorded below.

## Observed production recovery

The first repair deployment `1789867629594` reached ready with no reported frontend/backend errors or QA snapshot network errors. Its repository commit is `e0bf1f599c926dfbbdef71cbeb178ef1520658a2`. Native E2E status was not returned; the executable GitHub production checks are recorded separately.

| Job                                                                                                        | Actual result                                                                                                                                      |
| ---------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| [Source Health 35481479946](https://github.com/Wesleyk31/Hire-Intelligence/actions/runs/35481479946)       | SUCCESS; 55 eligible sources checked, zero failed, including Melbourne permits and AEMO KCI                                                        |
| [Live Refresh 35481510334](https://github.com/Wesleyk31/Hire-Intelligence/actions/runs/35481510334)        | SUCCESS; three Melbourne feeds fetched 120 records; no reported persistence failures                                                               |
| [Historical Backfill 35481507983](https://github.com/Wesleyk31/Hire-Intelligence/actions/runs/35481507983) | SUCCESS; bounded archive identity-index maintenance. No new archive page/evidence ingested in this run; source cursor remained 10,000              |
| [Source Validation 35481505600](https://github.com/Wesleyk31/Hire-Intelligence/actions/runs/35481505600)   | FAILED; a completed candidate-quality rejection was incorrectly reported as operational HTTP 503. Follow-up repair and verification recorded below |

The validation failure was reproduced using the real candidate probe modules against eight official HTTP 200 responses. Logan returned 20 rows: 19 carried publisher `INVALID_INPUT` rejection markers and were quarantined; one lacked a commercial signal. Its parser check correctly remained FAIL. Queensland coordinated projects returned 20 accepted rows. Both candidates still require admission review; freshness, rate-limit and reproducibility checks remain unverified. No source activation or invalid-record ingestion was performed.

[Production QA 35481477781](https://github.com/Wesleyk31/Hire-Intelligence/actions/runs/35481477781) also succeeded: GitHub acceptance, real production API/stored evidence, public browser/authentication boundary and persisted QA/E2E reports. Its health snapshot confirmed 55 ACTIVE sources, zero DEGRADED, four REVIEW_REQUIRED entries (two upstream access holds and two candidates) and 13 unobserved configured sources. Archive identity indexing advanced to three pages with no invalid records; it is not yet complete.

## Candidate-validation reporting repair

The follow-up changes only the internal automation API. A completed probe and successfully persisted admission decision now count as successful validation execution, including a correctly held unsuitable candidate. Current-invocation tracking prevents stale checks from being treated as new validation. Failed retrieval/access, thrown probes, failed discovery, failed decision persistence and non-executed validation still return FAILED/503. Output preserves the failed candidate checks and hold reasons and explicitly reports held candidates and zero activations. No admission criterion changed.

Preflight: the applied automation API snapshot was reread at `1789867629594`; only its incremental diff will be deployed. There is no user-visible workflow change or new SDK usage, resource or migration. Nine integration regressions cover held quality rejection, provider failure, all three access failures, discovery failure, persistence failure and two stale/non-execution cases. Full verification passed 452 unit tests, backend types, workflow/schema checks and independent review; the unchanged frontend retains its 117 passing browser checks. Production writers were paused for this follow-up deployment.

## Final validation release

Backend release `1789868107373` reached ready with no reported frontend/backend or QA snapshot network errors. It matches code commit `5bedb28`; the unchanged frontend was already covered by the 117 browser checks. The public host preflight passed again.

[Source Validation 35481819416](https://github.com/Wesleyk31/Hire-Intelligence/actions/runs/35481819416) succeeded against the deployed backend. Its persisted result reports two completed validations, zero execution failures, two held candidates and zero activations; discovery also succeeded. Logan's parser remains FAIL and both candidates remain REVIEW_REQUIRED with collection blocked. This verifies the intended distinction using real production data.

[Final Production QA 35481819186](https://github.com/Wesleyk31/Hire-Intelligence/actions/runs/35481819186) passed on code commit `5bedb28`: code acceptance, the real production API and stored evidence checks, public browser/authentication boundary, and QA/E2E result persistence all succeeded.

[Workflow Watchdog 35481907866](https://github.com/Wesleyk31/Hire-Intelligence/actions/runs/35481907866) then passed and persisted its observations. All six workflows were verified ACTIVE with `HI_PRODUCTION_AUTOMATION_ENABLED=true`. Economy schedules are restored; native AppDeploy crons remain absent. Historical failed runs are retained as incident evidence. No account-wide notification preference was changed.

Remaining limits: two upstream access-challenge holds, two unapproved candidates, 13 unobserved configured sources, and incomplete legacy archive indexing. These are disclosed rather than represented as recovered feeds or new archive ingestion. The final evidence-only documentation commit does not change the tested/deployed code.

## Capacity

Economy mode reduces scheduled executions by approximately 91%. The provider previously reported a 100-credit daily free allowance and a 14-credit deployment minimum, but did not expose per-operation pricing. This cadence is not a measured spending guarantee. Hosting preflight still stops a run on platform unavailability.
